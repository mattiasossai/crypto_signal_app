name: test

on: workflow_dispatch
jobs:
  where-am-i:
    runs-on: self-hosted
    steps:
      - name: Print cwd & disk usage
        run: |
          echo "PWD = $(pwd)"
          df -h .
          ls -ld .
