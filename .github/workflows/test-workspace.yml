# .github/workflows/test-workspace.yml
name: "🧭 Test Runner Workspace"

on:
  workflow_dispatch:

jobs:
  where-am-i:
    name: Wo schreibe ich?
    runs-on: self-hosted
    steps:
      - name: Print cwd & disk usage
        run: |
          echo "→ PWD    = $(pwd)"
          echo "→ ls -ld = $(ls -ld .)"
          echo "→ df -h  ="; df -h .
