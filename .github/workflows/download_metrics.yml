name: Download 4yr Metrics via Proxy (parallel)

on:
  workflow_dispatch:

jobs:
  download_metrics:
    name: Download ${{ matrix.metric }} | ${{ matrix.part }} | ${{ matrix.symbol }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        metric: [open_interest, funding_rate, liquidity]
        part:   [part1, part2]
        symbol: [BTCUSDT, ETHUSDT, BNBUSDT, SOLUSDT, XRPUSDT, ENAUSDT]
      max-parallel: 36

    env:
      # Dein Railway-Proxy:
      PROXY_URL: https://cryptosignalapp-production.up.railway.app
      GIT_USER: github-actions[bot]
      GIT_EMAIL: github-actions[bot]@users.noreply.github.com

    steps:
      - name: Checkout mit Schreibrechten
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: jq installieren
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Metrics herunterladen
        shell: bash
        run: |
          # Zeitraum für part1 vs. part2 festlegen
          if [[ "${{ matrix.part }}" == "part1" ]]; then
            START="2020-01-01"; END="2022-12-31"
          else
            START="2023-01-01"; END=$(date -I -d "yesterday")
          fi

          chmod +x metrics/download_metrics_range.sh
          PROXY_URL="${PROXY_URL}" \
            metrics/download_metrics_range.sh \
              "${{ matrix.metric }}" \
              "$START" \
              "$END" \
              "${{ matrix.part }}" \
              "${{ matrix.symbol }}"

      - name: Änderungen committen & pushen (robust)
        shell: bash
        run: |
          git config user.name "${GIT_USER}"
          git config user.email "${GIT_EMAIL}"

          # Remote-Stand frisch holen
          git fetch origin main
          # Setze Arbeitskopie & Index auf origin/main (eigene JSONs bleiben erhalten)
          git reset --mixed origin/main

          # Stage alle JSONs für dieses Symbol/Metric/Part
          git add metrics/${{ matrix.part }}/${{ matrix.metric }}/${{ matrix.symbol }}_*.json

          if ! git diff --cached --quiet; then
            git commit -m "chore(metrics): update ${{ matrix.metric }}/${{ matrix.part }}/${{ matrix.symbol }} [skip ci]"
            git push origin HEAD:main
          else
            echo "🚀 Keine Änderungen für ${{ matrix.metric }}/${{ matrix.part }}/${{ matrix.symbol }}"
          fi
