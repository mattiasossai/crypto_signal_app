name: 📥 Download & Commit metrics

on:
  workflow_dispatch:
  schedule:
    # täglich 02:00 UTC
    - cron: '0 2 * * *'

jobs:
  download:
    name: ⬇️ Download metrics | ${{ matrix.symbol }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        symbol: [BTCUSDT, ETHUSDT, BNBUSDT, SOLUSDT, XRPUSDT, ENAUSDT]
      max-parallel: 6

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install unzip & curl
        run: sudo apt-get update && sudo apt-get install -y unzip curl

      - name: Download & extract metrics (Backfill)
        shell: bash
        run: |
          set -euo pipefail
          SYMBOL=${{ matrix.symbol }}

          # yesterday
          YESTERDAY=$(date -I -d "yesterday")
          TARGET_DIR="historical_tech/metrics/${SYMBOL}"
          if compgen -G "$TARGET_DIR/${SYMBOL}-metrics-*.csv" > /dev/null; then
            LAST=$(find "$TARGET_DIR" -type f -name "${SYMBOL}-metrics-*.csv" \
              | sed -E "s/.*${SYMBOL}-metrics-([0-9]{4}-[0-9]{2}-[0-9]{2})\.csv/\1/" \
              | sort | tail -n1)
            START=$(date -d "$LAST +1 day" '+%Y-%m-%d')
          else
            START="$YESTERDAY"
          fi
          END="$YESTERDAY"

          BASE="data/futures/um/daily/metrics/${SYMBOL}"
          mkdir -p "$BASE"

          cur="$START"
          while [[ "$cur" < "$END" || "$cur" == "$END" ]]; do
            DATE="$cur"
            ZIP="${SYMBOL}-metrics-${DATE}.zip"
            URL="https://data.binance.vision/data/futures/um/daily/metrics/${SYMBOL}/${ZIP}"
            echo "→ Fetching metrics $DATE"
            if curl -sSf "$URL" -o tmp.zip; then
              unzip -p tmp.zip > "$BASE/${ZIP%.zip}.csv"
              rm tmp.zip
              echo " ✅ extracted ${ZIP}"
            else
              echo " ⚠️ missing ${ZIP}"
              rm -f tmp.zip || true
            fi
            cur=$(date -I -d "$cur +1 day")
          done

      - name: Upload metrics artifact
        uses: actions/upload-artifact@v4
        with:
          name: data-metrics-${{ matrix.symbol }}
          path: data/futures/um/daily/metrics/${{ matrix.symbol }}/*

  aggregate_and_push:
    name: Aggregate & Commit metrics Data
    needs: download
    runs-on: ubuntu-latest

    steps:
      - name: Checkout fresh
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Download metrics artifacts
        uses: actions/download-artifact@v4
        with:
          path: data-artifacts

      - name: Merge metrics into historical_tech
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p historical_tech/metrics
          find data-artifacts -type f | while read src; do
            rel=${src#data-artifacts/data/futures/um/daily/metrics/}
            dst="historical_tech/metrics/${rel}"
            mkdir -p "$(dirname "$dst")"
            mv "$src" "$dst"
          done

      - name: Commit & Push metrics
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --mixed origin/main
          git add historical_tech/metrics
          if git diff --cached --quiet; then
            echo "✅ No new metrics to commit"
          else
            git commit -m "chore(data): backfill metrics ${START}→${END} [skip ci]"
            git push origin HEAD:main
          fi
